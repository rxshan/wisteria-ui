import { Surface, Space } from 'wisteria-ui';
import { Tabs, Tab } from 'nextra/components';
import { Box, ConfigAPI, CodeDemoArea } from '@/components';

# Paper

```tsx
import { Surface } from 'wisteria-ui';
```

## 用法

### Variant

```tsx
<Surface variant="outlined">
  <Box width={180} height={80}>Outlined</Box>
</Surface>

<Surface variant="elevated" elevation={3}>
  <Box width={180} height={80}>Elevated</Box>
</Surface>
```

<CodeDemoArea label="预览">
  <Space size={24}>
    <Surface variant="outlined" corner={12}>
      <Box
        width={180}
        height={80}
        className="bg-transparent"
      >
        Outlined
      </Box>
    </Surface>
    <Surface variant="elevated" elevation={3} corner={12}>
      <Box
        width={180}
        height={80}
        className="bg-transparent"
      >
        Elevated
      </Box>
    </Surface>

  </Space>
</CodeDemoArea>

### Elevation

```tsx
Array.from({ length: 6 }, (_, index) => index).map(elevation => (
  <Surface elevation={elevation}>
    <Box width={180} height={80} className="bg-transparent">
      {`elevation-${elevation}`}
    </Box>
  </Surface>
));
```

<CodeDemoArea label="预览">
  <Space wrap size={24}>
    {Array.from({ length: 6 }, (_, index) => index).map(elevation => (
      <Surface elevation={elevation}>
        <Box width={180} height={80} className="bg-transparent">
          {`elevation-${elevation}`}
        </Box>
      </Surface>
    ))}
  </Space>
</CodeDemoArea>

### Corner

```tsx
<Surface corner="999px" elevation={3}>
  <Box>Corner - Full</Box>
</Surface>
```

<CodeDemoArea label="预览">
  <Space size={24}>
    <Surface corner="0" elevation={3}>
      <Box width={180} height={80} className="bg-transparent rounded-none">
        Corner - None
      </Box>
    </Surface>
    <Surface corner="16px" elevation={3}>
      <Box width={180} height={80} className="bg-transparent">
        Corner - 16px
      </Box>
    </Surface>
    <Surface corner="999px" elevation={3}>
      <Box width={180} height={80} className="bg-transparent">
        Corner - Full
      </Box>
    </Surface>
  </Space>
</CodeDemoArea>

## API

<ConfigAPI
  list={[
    {
      property: 'corner',
      description: '指定圆角大小',
      typedef: 'number | string'
    },
    {
      property: 'elevation',
      description: '指定阴影层级，0～5',
      typedef: 'number',
      defaultValue: '1'
    },
    {
      property: 'variant',
      description: '变体类型',
      typedef: 'elevation | outlined',
      defaultValue: 'elevation'
    },
    {
      property: 'bgColor',
      description: '背景颜色',
      typedef: 'string',
      defaultValue: 'colors.surface'
    }

]}
/>
